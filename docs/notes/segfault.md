# Segmentation Fault (segfault)

> access violation

## Notes

A segfault is a failure condition that is raised by a computer's hardware with memory protection, notifying the OS that a particular software has attempted to access a restricted area of memory (i.e. a memory access violation).

> Memory protection is a way to control memory access rights on a computer, and is a part of most modern instruction set architectures and operating systems. The main purpose of memory protection is to prevent a process from accessing memory that has not been allocated to it.

## References

- https://en.wikipedia.org/wiki/Segmentation_fault
- https://en.wikipedia.org/wiki/Memory_protection